spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
     active: native
#  security:
#    user:
#      password: ${CONFIG_SERVICE_PASSWORD}

  application:
    name: config-service

server:
  port: 9190
  
    

